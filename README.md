# omniSAM clean E1 launcher

Tiny bootstrap repo for RunAI jobs.

It exists only to keep `git-sync` fast and reliable. The launcher downloads the
full clean-reruns repository from GitHub at runtime, unpacks it, and executes
the clean E1 trainer from that checkout.
